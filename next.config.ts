import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["cms.nuyu-dental.co.uk"], // Add the domain where your images are hosted
  },
};

export default nextConfig;
